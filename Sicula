-- BAVOHUB - Ultimate Survival Script
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "BAVOHUB",
   LoadingTitle = "Đang tải BAVOHUB...",
   LoadingSubtitle = "by KING",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local lp = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- ==================== BIẾN ====================
local FlyEnabled, FlySpeed = false, 80
local NoclipEnabled = false
local SpeedEnabled, WalkSpeedValue = false, 45
local AutoPickupEnabled, PickupDelay, MaxPickupDistance = false, 0.025, 35
local KillAuraEnabled, KillAuraRange, KillAuraDamage = false, 20, 40
local AimbotEnabled, AimbotFOV = false, 200
local HitboxEnabled, HitboxSize = false, 15
local GodModeEnabled = false
local InfiniteJumpEnabled = false
local AutoHealEnabled = false
local NoFallDamage = false
local SkipDayEnabled = false
local SelectedFilters = {}

local function isInFilter(name)
    if #SelectedFilters == 0 then return true end
    name = name:lower()
    for _, f in ipairs(SelectedFilters) do
        if name:find(f) then return true end
    end
    return false
end

-- ==================== TẠO TABS ====================
local TabESP = Window:CreateTab("ESP", 4483362458)
local TabCombat = Window:CreateTab("Chiến đấu", 4483362458)
local TabMove = Window:CreateTab("Chuyển động", 4483362458)
local TabPickup = Window:CreateTab("Lấy Đồ", 4483362458)
local TabFarm = Window:CreateTab("Auto Farm", 4483362458)
local TabMisc = Window:CreateTab("Misc", 4483362458)

-- ==================== ESP ====================
TabESP:CreateSection("ESP")
TabESP:CreateToggle({Name = "Player ESP", CurrentValue = true, Callback = function(v) print("Player ESP:", v) end})
TabESP:CreateToggle({Name = "Zombie ESP", CurrentValue = true, Callback = function(v) print("Zombie ESP:", v) end})
TabESP:CreateToggle({Name = "Item ESP", CurrentValue = true, Callback = function(v) print("Item ESP:", v) end})
TabESP:CreateToggle({Name = "Box + Health", CurrentValue = true, Callback = function(v) print("Box:", v) end})
TabESP:CreateToggle({Name = "Tracers", CurrentValue = true, Callback = function(v) print("Tracers:", v) end})

-- ==================== CHIẾN ĐẤU ====================
TabCombat:CreateSection("Combat")
TabCombat:CreateToggle({Name = "Aimbot Silent", CurrentValue = false, Callback = function(v) AimbotEnabled = v end})
TabCombat:CreateSlider({Name = "Aimbot FOV", Range = {50, 900}, Increment = 10, CurrentValue = 200, Callback = function(v) AimbotFOV = v end})

TabCombat:CreateToggle({Name = "Kill Aura", CurrentValue = false, Callback = function(v) KillAuraEnabled = v end})
TabCombat:CreateSlider({Name = "Kill Aura Range", Range = {5, 60}, Increment = 1, CurrentValue = 20, Callback = function(v) KillAuraRange = v end})
TabCombat:CreateSlider({Name = "Kill Aura Damage", Range = {10, 100}, Increment = 5, CurrentValue = 40, Callback = function(v) KillAuraDamage = v end})

TabCombat:CreateToggle({Name = "Hitbox Expander", CurrentValue = false, Callback = function(v) HitboxEnabled = v end})
TabCombat:CreateSlider({Name = "Hitbox Size", Range = {5, 50}, Increment = 1, CurrentValue = 15, Callback = function(v) HitboxSize = v end})

TabCombat:CreateToggle({Name = "God Mode", CurrentValue = false, Callback = function(v) GodModeEnabled = v end})

-- ==================== CHUYỂN ĐỘNG ====================
TabMove:CreateSection("Movement")
TabMove:CreateToggle({Name = "Fly", CurrentValue = false, Callback = function(v) FlyEnabled = v end})
TabMove:CreateSlider({Name = "Fly Speed", Range = {20, 500}, Increment = 5, CurrentValue = 80, Callback = function(v) FlySpeed = v end})

TabMove:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(v) NoclipEnabled = v end})
TabMove:CreateToggle({Name = "Speed Hack", CurrentValue = false, Callback = function(v) SpeedEnabled = v end})
TabMove:CreateSlider({Name = "WalkSpeed", Range = {16, 250}, Increment = 1, CurrentValue = 45, Callback = function(v) WalkSpeedValue = v end})
TabMove:CreateToggle({Name = "Infinite Jump", CurrentValue = false, Callback = function(v) InfiniteJumpEnabled = v end})
TabMove:CreateToggle({Name = "No Fall Damage", CurrentValue = false, Callback = function(v) NoFallDamage = v end})

-- ==================== LẤY ĐỒ ====================
TabPickup:CreateSection("Auto Pickup")
TabPickup:CreateToggle({Name = "Bật Auto Pickup", CurrentValue = false, Callback = function(v) AutoPickupEnabled = v end})
TabPickup:CreateSlider({Name = "Delay (giây)", Range = {0.01, 0.15}, Increment = 0.01, CurrentValue = 0.025, Callback = function(v) PickupDelay = v end})
TabPickup:CreateSlider({Name = "Khoảng cách", Range = {10, 80}, Increment = 1, CurrentValue = 35, Callback = function(v) MaxPickupDistance = v end})

TabPickup:CreateSection("Filter Đồ")
TabPickup:CreateToggle({Name = "Vũ khí", CurrentValue = false, Callback = function(v) if v then table.insert(SelectedFilters,"gun") table.insert(SelectedFilters,"weapon") end end})
TabPickup:CreateToggle({Name = "Thức ăn / Nước", CurrentValue = false, Callback = function(v) if v then table.insert(SelectedFilters,"food") table.insert(SelectedFilters,"water") end end})
TabPickup:CreateToggle({Name = "Đạn", CurrentValue = false, Callback = function(v) if v then table.insert(SelectedFilters,"ammo") end end})
TabPickup:CreateToggle({Name = "Vật liệu / Tool", CurrentValue = false, Callback = function(v) if v then table.insert(SelectedFilters,"material") table.insert(SelectedFilters,"tool") end end})

-- ==================== AUTO FARM ====================
TabFarm:CreateSection("Auto Farm")
TabFarm:CreateToggle({Name = "Auto Farm Quái", CurrentValue = false, Callback = function(v) print("Auto Farm:", v) end})
TabFarm:CreateToggle({Name = "Auto Heal", CurrentValue = false, Callback = function(v) AutoHealEnabled = v end})

-- ==================== MISC + SKIP DAY ====================
TabMisc:CreateSection("Misc")
TabMisc:CreateToggle({Name = "Skip Day (Trời sáng ngay)", CurrentValue = false, Callback = function(v)
    SkipDayEnabled = v
    if v then
        Lighting.ClockTime = 14
        Lighting.Brightness = 3
        Lighting.FogEnd = 999999
        print("🌞 Skip Day Activated")
    end
end})

TabMisc:CreateButton({Name = "Force Skip Day", Callback = function()
    Lighting.ClockTime = 14
    Lighting.Brightness = 3
    Lighting.FogEnd = 999999
    Rayfield:Notify({Title = "Skip Day", Content = "Trời đã được buộc sáng!", Duration = 5})
end})

TabMisc:CreateToggle({Name = "Fullbright", CurrentValue = true, Callback = function(v) 
    if v then 
        Lighting.Brightness = 3 
        Lighting.FogEnd = 999999 
    end 
end})

TabMisc:CreateToggle({Name = "Anti AFK", CurrentValue = true, Callback = function() end})
TabMisc:CreateButton({Name = "Rejoin Server", Callback = function() 
    game:GetService("TeleportService"):Teleport(game.PlaceId) 
end})

-- ==================== NOTIFY ====================
Rayfield:Notify({
   Title = "BAVOHUB",
   Content = "Menu đã load xong!\nBật Skip Day khi trời tối để chuyển sáng ngay.",
   Duration = 8
})

print("✅ BAVOHUB loaded!")

-- ==================== LOGIC CHẠY ====================

RunService.Heartbeat:Connect(function()
    local char = lp.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not root or not hum then return end

    -- Speed Hack
    if SpeedEnabled then 
        hum.WalkSpeed = WalkSpeedValue 
    else 
        hum.WalkSpeed = 16 
    end

    -- Fly
    if FlyEnabled and root then
        local move = Vector3.new()
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then move += camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then move -= camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then move -= camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then move += camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move += Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then move -= Vector3.new(0,1,0) end
        if move.Magnitude > 0 then root.Velocity = move.Unit * FlySpeed end
    end

    -- Noclip
    if NoclipEnabled then
        for _, p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    end

    -- God Mode
    if GodModeEnabled and hum then 
        hum.Health = hum.MaxHealth 
    end

    -- Infinite Jump
    if InfiniteJumpEnabled and UserInputService:IsKeyDown(Enum.KeyCode.Space) then
        hum:ChangeState(Enum.HumanoidStateType.Jumping)
    end

    -- No Fall Damage
    if NoFallDamage and hum:GetState() == Enum.HumanoidStateType.Freefall then
        hum:ChangeState(Enum.HumanoidStateType.Landed)
    end

    -- Skip Day
    if SkipDayEnabled then
        Lighting.ClockTime = 14
        Lighting.Brightness = 3
    end
end)

-- Auto Pickup
local lastPickup = 0
RunService.Heartbeat:Connect(function()
    if not AutoPickupEnabled or tick() - lastPickup < PickupDelay then return end
    local char = lp.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local rootPos = char.HumanoidRootPart.Position

    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") and obj.Enabled and obj.Parent then
            local dist = (obj.Parent.Position - rootPos).Magnitude
            if dist <= MaxPickupDistance and isInFilter(obj.Parent.Name) then
                pcall(function() fireproximityprompt(obj) end)
            end
        end
    end
    lastPickup = tick()
end)

-- Kill Aura
RunService.Heartbeat:Connect(function()
    if not KillAuraEnabled then return end
    local char = lp.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local rootPos = char.HumanoidRootPart.Position

    for _, mob in ipairs(workspace:GetDescendants()) do
        if mob:FindFirstChild("Humanoid") and mob:FindFirstChild("HumanoidRootPart") and mob \~= char then
            if (mob.HumanoidRootPart.Position - rootPos).Magnitude <= KillAuraRange then
                pcall(function() mob.Humanoid:TakeDamage(KillAuraDamage) end)
            end
        end
    end
end)

-- Hitbox Expander
RunService.Heartbeat:Connect(function()
    if not HitboxEnabled then return end
    for _, mob in ipairs(workspace:GetDescendants()) do
        if mob:FindFirstChild("Humanoid") and mob:FindFirstChild("HumanoidRootPart") and mob \~= lp.Character then
            local r = mob.HumanoidRootPart
            r.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
            r.Transparency = 0.5
            r.CanCollide = false
        end
    end
end)

print("🚀 BAVOHUB sẵn sàng sử dụng!")
